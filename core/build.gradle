apply plugin: 'java-library'
apply plugin: 'kotlin'
apply plugin: 'kotlinx-serialization'
//Standalone Android Lint, shipped inside AGP. Needed because :core is a plain java-library,
//so :android's lint checkDependencies does NOT descend into it -- verified: with
//checkDependencies=true, lint analysed 9 android files and zero of core's 1068.
//This is also the only static analysis that reads the 49 Kotlin files, since options.release
//has no Kotlin equivalent.
apply plugin: 'com.android.lint'

lint {
    //Ratchet, not a cliff: the 12 inherited DefaultLocale findings are parked in the baseline
    //so NEW violations fail the build while the existing backlog is worked down separately.
    //
    //A baseline is itself a gate that can lie -- it hides everything recorded in it. Review it
    //on a schedule and shrink it; do not let it grow. Deleting the file and re-running
    //regenerates it, which silently forgives regressions, so treat it as a tracked artifact.
    baseline = file('lint-baseline.xml')
    abortOnError = true
    warningsAsErrors = true
}

[compileJava, compileTestJava]*.options*.encoding = 'UTF-8'
sourceCompatibility = targetCompatibility = appJavaCompatibility

dependencies {
    api project(':SPD-classes')
    implementation project(':services')
    implementation "org.jetbrains.kotlin:kotlin-stdlib-jdk8:$kotlin_version"
    implementation "org.jetbrains.kotlinx:kotlinx-serialization-json:1.5.0"
    implementation "org.jetbrains.kotlinx:kotlinx-coroutines-core:1.6.4"
    implementation "org.slf4j:slf4j-simple:1.7.25"
    implementation 'io.ktor:ktor-client-core-jvm:2.3.0'
    implementation 'io.ktor:ktor-client-okhttp-jvm:2.3.0'
    implementation 'io.ktor:ktor-client-logging-jvm:2.3.0'
    implementation 'io.ktor:ktor-client-content-negotiation:2.3.0'
    implementation 'io.ktor:ktor-serialization-kotlinx-json:2.3.0'
}

buildscript {
    ext.kotlin_version = '1.8.10'
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"
        classpath "org.jetbrains.kotlin:kotlin-serialization:$kotlin_version"
    }
}

compileKotlin {
    sourceCompatibility = JavaVersion.VERSION_1_8
    targetCompatibility = JavaVersion.VERSION_1_8

    kotlinOptions {
        jvmTarget = "1.8"
    }
}
